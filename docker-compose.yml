version: '3'
services:
  web:
    build: .
    restart: always
    env_file: .env
    environment:
      DATABASE_URL: postgres://postgres@postgres/chatroom
    depends_on:
      - db
    ports:
    - "3000:3000"
  postgres:
    image: "postgres"
    restart: always
    ports:
    - "5432:5432"
